# SelfGNN (Base + Feature): training, analysis, preprocessing notebook
# Install PyTorch with the right CUDA build from https://pytorch.org/ if needed.
numpy>=1.24,<3
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn[stats]>=0.13
torch>=2.0
jupyter>=1.0.0
